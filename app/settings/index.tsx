import { PlaceholderScreen } from '@/components/ui';

export default function SettingsScreen() {
  return (
    <PlaceholderScreen
      title="Settings"
      description="Units, logout, and account deletion will be added with profile settings."
    />
  );
}
