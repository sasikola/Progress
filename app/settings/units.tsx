import { PlaceholderScreen } from '@/components/ui';

export default function UnitsScreen() {
  return (
    <PlaceholderScreen
      title="Units"
      description="Weight, height, and measurement units will be configurable in settings."
    />
  );
}
