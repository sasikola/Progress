import { PlaceholderScreen } from '@/components/ui';

export default function OnboardingCompleteScreen() {
  return (
    <PlaceholderScreen
      title="You're ready"
      description="Onboarding completion will route into the main app in a later milestone."
    />
  );
}
