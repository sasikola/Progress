import { PlaceholderScreen } from '@/components/ui';

export default function OnboardingProfileScreen() {
  return (
    <PlaceholderScreen
      title="Profile"
      description="Onboarding will collect name, date of birth, and height in a later milestone."
    />
  );
}
