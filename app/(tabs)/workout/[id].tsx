import { PlaceholderScreen } from '@/components/ui';

export default function WorkoutDetailScreen() {
  return (
    <PlaceholderScreen
      title="Workout detail"
      description="Read-only workout detail will be added with history."
    />
  );
}
