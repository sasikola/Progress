import { PlaceholderScreen } from '@/components/ui';

export default function RecordsScreen() {
  return (
    <PlaceholderScreen
      title="Personal records"
      description="PRs are the highest completed non-warmup weight and will be derived from sets."
    />
  );
}
