import { PlaceholderScreen } from '@/components/ui';

export default function AddPhotoScreen() {
  return (
    <PlaceholderScreen
      title="Add photo"
      description="Camera and gallery upload will be added later. Photos will stay private."
    />
  );
}
